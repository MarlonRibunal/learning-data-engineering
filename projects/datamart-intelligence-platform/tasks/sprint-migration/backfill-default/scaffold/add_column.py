def add_column(rows, column, default):
    # TODO: ensure every row has `column` — keep the existing value where
    # present, fill `default` only where it's missing. Don't clobber real data.
    return rows
