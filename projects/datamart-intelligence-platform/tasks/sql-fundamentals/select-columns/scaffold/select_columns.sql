-- Task: select specific columns
--
-- Return the order_id, status, and total_amount for every order.
-- Name the three columns explicitly (don't use SELECT *).
--
-- The orders table has: order_id, customer_id, order_date, total_amount, status
--
-- TODO: write your query below.
SELECT 1;
