-- Task: orders above average (subquery)
--
-- Return the orders whose total_amount is greater than the AVERAGE total_amount
-- across all orders. You'll compute the average with a subquery — a query nested
-- inside another, in parentheses.
--
-- TODO: write your query below.
SELECT 1;
