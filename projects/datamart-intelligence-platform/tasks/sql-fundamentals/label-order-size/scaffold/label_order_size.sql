-- Task: label order size (CASE)
--
-- Show order_id and a label 'large' when total_amount >= 100, otherwise 'small'.
--
-- A CASE expression is SQL's if/else:
--   CASE WHEN <condition> THEN <value> ELSE <value> END
--
-- TODO: write your query below.
SELECT 1;
