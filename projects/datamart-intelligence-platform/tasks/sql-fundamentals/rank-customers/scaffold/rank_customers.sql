-- Task: rank customers by spend (window function)
--
-- Show customer_id, their total spend, and a rank (1 = highest spender).
-- Use a WINDOW function: RANK() OVER (ORDER BY ...). Unlike GROUP BY alone, a
-- window function adds a ranking column without collapsing your rows away.
--
-- TODO: write your query below.
SELECT 1;
