-- Task: top 3 customers by spend
--
-- Show each customer's name and their total spend, highest first, limited to 3.
-- The revenue is in orders; the name is in customers, so you'll JOIN them.
--
--   orders:    order_id, customer_id, order_date, total_amount, status
--   customers: customer_id, customer_name, email, ...
--
-- TODO: write your query below.
SELECT 1;
