**Ingestion is the front of the data engineering lifecycle** — getting data *in*.
Raw feeds are messy: duplicates, updates, late arrivals. Here you'll load a raw
product feed into a clean table, and learn the habit that separates a script from a
pipeline: making a load safe to re-run.
