# Task: consume order events from a Redpanda topic
#
# Write consume() so it reads ALL messages currently on `topic` and returns them
# as a list of dicts. The grader produces a known set of events first, then checks
# what you return.
#
# TODO: read the messages.

def consume(bootstrap_servers, topic):
    return []
