# Task: produce order events to a Redpanda topic
#
# Write produce() so it sends each event in `events` to `topic`, using a
# KafkaProducer that serializes each event to JSON. The grader then consumes the
# topic and checks your events arrived.
#
# TODO: send each event.

def produce(bootstrap_servers, topic, events):
    pass
