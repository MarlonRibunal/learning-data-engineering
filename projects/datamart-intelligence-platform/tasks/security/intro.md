**Security is the undercurrent that runs through every stage of the lifecycle.** Two
of its most everyday habits: give people only the access they need (least privilege),
and don't expose sensitive data you don't have to. Here you set up a read-only role
and build a PII-safe view — the kind of controls a data engineer owns.
