def unrecovered(expected, actual):
    # TODO: return the sorted names of tables whose actual count doesn't match
    # expected (use actual.get(name) so a missing table counts as unrecovered).
    return []
