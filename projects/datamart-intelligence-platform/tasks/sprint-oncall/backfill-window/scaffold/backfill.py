from datetime import date, timedelta


def backfill_dates(last_success, today):
    # TODO: return the ISO dates to reprocess — the day AFTER last_success
    # through today, inclusive. Mind both boundaries (no gap, no double-count).
    return []
