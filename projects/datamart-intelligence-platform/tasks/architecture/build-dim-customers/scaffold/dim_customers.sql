-- Task: build a customer dimension
--
-- A dimension table describes a thing (a customer) with one clean row per business
-- key. Fill analytics.dim_customers(customer_id, customer_name, email) from
-- raw.customers.
--
-- TODO: write the INSERT.
SELECT 1;
