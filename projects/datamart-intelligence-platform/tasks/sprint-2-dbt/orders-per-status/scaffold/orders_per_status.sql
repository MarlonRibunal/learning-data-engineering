-- SCAFFOLD (exercise stub) for sprint-2-dbt / orders-per-status.
-- Reset with: ./scripts/check.sh start sprint-2-dbt orders-per-status
-- TODO: one row per status with the number of orders in that status (COUNT).
SELECT
    'TODO' AS status,
    0 AS order_count
