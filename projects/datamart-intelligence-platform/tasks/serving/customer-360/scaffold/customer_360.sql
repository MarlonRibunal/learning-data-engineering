-- Task: build a customer 360 serving table
--
-- The empty table analytics.customer_360(customer_name, order_count, total_spend)
-- is ready. Fill it with one row per customer: their name, how many orders they
-- placed, and their total spend. Customers with no orders should show 0.
--
-- Read from raw.customers and raw.orders (use a LEFT JOIN so every customer appears).
--
-- TODO: write the INSERT.
SELECT 1;
