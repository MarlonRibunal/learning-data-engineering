def moving_average(values, w):
    # TODO: return the trailing moving average of window w — each point is the
    # mean of itself and up to w-1 previous points, rounded to 2 decimals.
    return values
