def retention_curve(cohort, activity):
    # TODO: for each day in `activity`, count how many `cohort` users were
    # active. Return {day: count}. A set intersection makes this clean.
    return {}
