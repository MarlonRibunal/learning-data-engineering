def topo_order(edges):
    # TODO: topological sort (Kahn's algorithm). Track each node's in-degree,
    # repeatedly emit a node with in-degree 0 (use a heap for an alphabetical
    # tiebreak), and decrement its neighbours. Return the run order.
    return []
