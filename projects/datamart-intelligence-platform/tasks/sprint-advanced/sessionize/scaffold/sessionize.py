def count_sessions(times, gap):
    # TODO: sort the times, then count how many sessions there are — a new
    # session starts whenever the gap between consecutive events exceeds `gap`.
    return 0
