def circuit_state(results, threshold):
    # TODO: count the consecutive "fail"s at the END of results; return "open"
    # if that streak >= threshold, else "closed". A single "ok" resets it.
    return "closed"
