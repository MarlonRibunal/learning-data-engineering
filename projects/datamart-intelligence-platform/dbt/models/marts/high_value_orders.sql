-- SCAFFOLD (exercise stub) for sprint-2-dbt / high-value-orders.
-- Reset with: ./scripts/check.sh start sprint-2-dbt high-value-orders
-- TODO: keep only the orders worth more than 100.
SELECT
    0 AS order_id,
    0 AS total_amount
