-- SCAFFOLD (exercise stub) for sprint-2-dbt / avg-order-value.
-- Reset with: ./scripts/check.sh start sprint-2-dbt avg-order-value
-- TODO: one row per status with the average order value (rounded to 2 places).
SELECT
    'TODO' AS status,
    0 AS avg_amount
